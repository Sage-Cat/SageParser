#include "OdooExportedProcessor.hpp"

namespace SageDocs
{
    std::shared_ptr<Dataset> OdooExportedProcessor::process(std::shared_ptr<Dataset> dataset)
    {
        // TODO: implementation
        return process(dataset);
    }
}